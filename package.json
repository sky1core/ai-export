{
  "name": "ai-export",
  "version": "0.7.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.27.2",
    "happy-dom": "^20.0.11",
    "typescript": "^5.7.0",
    "vitest": "^4.0.16"
  }
}
